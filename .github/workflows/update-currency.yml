name: Update Currency Data

on:
  workflow_dispatch:
  schedule:
  - cron: '0 */12 * * *'  # Runs every 30 minutes 

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Initialize Go Module (if needed)
        run: go mod init Chand-api || true


      - name: run
        run: |
          git clone https://github.com/LonamiWebs/Telethon ; cd Telethon* ; cp -r * ../ ; rm -rf Telethon*
          cd telethon ; wget https://raw.githubusercontent.com/amirwolf5122/data/newmm1/fast.py
          
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --quiet && git diff --staged --quiet || (git commit -m "🚀 Updated - $(TZ='Asia/Tehran' date '+%Y-%m-%d %H:%M:%S')" && git push)
